# mem_pan_app
scrcpy


git add .
git commit -m "Update feature"
git tag v1.0.3

git push origin main      # Push code
git push origin v1.0.3    # Push tag -> Workflow chạy


Trường hợp trả lời Đúng: Điểm số được chia theo thời gian trả lời kể từ lúc câu hỏi xuất hiện trên màn hình:

Dưới 3 giây (< 3000 ms): Đạt điểm 4 (Easy).
Từ 3 giây đến dưới 8 giây (3000 ms ≤ t < 8000 ms): Đạt điểm 3 (Good).
Từ 8 giây trở lên (≥ 8000 ms): Đạt điểm 2 (Hard).